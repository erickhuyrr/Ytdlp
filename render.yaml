services:
  - type: web
    name: ytdlp-api
    env: node
    region: singapore
    plan: free
    buildCommand: |
      apt-get update
      apt-get install -y ffmpeg
      npm install
    startCommand: npm start
    autoDeploy: true
